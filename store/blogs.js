import blogs from '~/store/api/v1/blogs.json'

const state = () => ({
  list: blogs
})

const getters = {}

const actions = {}

const mutations = {}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
